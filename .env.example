# Root .env.example
# Copy this file to each app and configure accordingly

# API (apps/api/.env)
MONGODB_URI=mongodb://localhost:27017/seethenumbers
PORT=4000
NODE_ENV=development

# Web (apps/web/.env.local)
NEXT_PUBLIC_API_URL=http://localhost:4000
